# 🎮 NeonVoid - Новые системы (Этап 1)

## ✅ Созданные системы:

### 1. **PowerUpManager** - Система Power-Ups
Полноценная система дропов и временных усилений.

**Возможности:**
- 10 типов power-ups (здоровье, щит, патроны, скорость, урон, огонь, неуязвимость и т.д.)
- Настраиваемый шанс дропа для каждого типа
- Временные баффы с таймерами
- Магнитный сбор предметов
- Плавающая анимация pickups
- Звуковые эффекты

**Как использовать:**
```csharp
// Спавн power-up при убийстве врага
PowerUpManager.Instance.TrySpawnPowerUp(enemyPosition);

// Спавн конкретного типа
PowerUpManager.Instance.SpawnPowerUp(PowerUpType.Health, position);

// Проверка активных баффов
if (PowerUpManager.Instance.IsPowerUpActive(PowerUpType.SpeedBoost))
{
    // Buff активен
}
```

**Настройка в Unity:**
1. Создайте пустой GameObject → Add Component → PowerUpManager
2. Назначьте префабы power-ups
3. Настройте шансы дропа и длительность эффектов
4. Создайте префаб с CircleCollider2D (Trigger) для pickup

---

### 2. **DashAbility** - Механика рывка
Dash с cooldown, ghost trail и invincibility frames.

**Возможности:**
- Рывок в направлении движения/прицеливания
- Система зарядов (можно несколько дашей)
- Ghost trail эффект
- Временная неуязвимость
- Настраиваемый cooldown
- Trail renderer поддержка

**Как использовать:**
```csharp
// Добавить компонент к Player
DashAbility dash = player.GetComponent<DashAbility>();

// Проверка доступности
if (dash.IsReady())
{
    dash.TryDash();
}

// Dash в определенном направлении
dash.DashInDirection(Vector2.right);

// События
dash.OnDashStart += () => Debug.Log("Dash started!");
dash.OnDashEnd += () => Debug.Log("Dash ended!");
```

**Настройка в Unity:**
1. Добавьте DashAbility к PlayerController
2. Настройте: скорость, длительность, cooldown
3. Назначьте клавишу (по умолчанию LeftShift)
4. Добавьте TrailRenderer (опционально)
5. Создайте ghost prefab (опционально)

---

### 3. **ParticleManager** - Управление эффектами
Централизованная система для всех particle effects.

**Возможности:**
- Object pooling для эффектов
- Библиотека предустановленных эффектов
- Screen shake система
- Легкое добавление новых эффектов
- Автоматическое управление lifetime

**Как использовать:**
```csharp
// Взрыв при смерти врага
ParticleManager.Instance.SpawnExplosion(position);

// Эффект попадания
ParticleManager.Instance.SpawnImpact(position);

// Кастомный эффект
ParticleManager.Instance.SpawnEffect("MyEffect", position);

// Screen shake
ParticleManager.Instance.ShakeScreen(0.3f, 0.5f);
```

**Настройка в Unity:**
1. Создайте GameObject → Add Component → ParticleManager
2. Назначьте prefabs для эффектов (Explosion, Impact, etc.)
3. Настройте pooling параметры

---

### 4. **ObjectPoolManager** - Object Pooling
Универсальная система пулов для оптимизации.

**Возможности:**
- Пулы для пуль, врагов, эффектов
- Автоматическое расширение пулов
- Pre-warming
- Статистика использования
- Runtime создание пулов

**Как использовать:**
```csharp
// Спавн пули из пула
GameObject bullet = ObjectPoolManager.Instance.SpawnBullet(position, rotation);

// Возврат в пул
ObjectPoolManager.Instance.Return(bullet);

// Авто-возврат через время
ObjectPoolManager.Instance.ReturnAfterDelay(bullet, 3f);

// Создание нового пула
ObjectPoolManager.Instance.CreateRuntimePool("MyPool", prefab, 20);
```

**Настройка в Unity:**
1. Создайте GameObject → Add Component → ObjectPoolManager
2. Добавьте пулы в массив Pools
3. Настройте initial/max size для каждого
4. Опционально назначьте автопулы (Bullet, Enemy, etc.)

---

## 📝 Интеграция с существующими системами:

### Интеграция Power-Ups с Enemy:
```csharp
// В Enemy.cs метод Die()
private void Die()
{
    // ... существующий код ...
    
    // Добавить spawn power-up
    if (PowerUpManager.Instance != null)
    {
        PowerUpManager.Instance.TrySpawnPowerUp(transform.position);
    }
    
    Destroy(gameObject);
}
```

### Интеграция Dash с PlayerController:
```csharp
// Dash уже работает как отдельный компонент
// Просто добавьте DashAbility к Player GameObject
```

### Интеграция ParticleManager с Enemy/Player:
```csharp
// В Enemy.cs
private void Die()
{
    if (ParticleManager.Instance != null)
    {
        ParticleManager.Instance.SpawnExplosion(transform.position);
        ParticleManager.Instance.ShakeScreen(0.2f, 0.3f);
    }
}

// В EnemyBullet.cs или PlayerHealth.cs
private void OnHit()
{
    if (ParticleManager.Instance != null)
    {
        ParticleManager.Instance.SpawnImpact(transform.position);
    }
}
```

### Интеграция Object Pooling:
```csharp
// Вместо Instantiate/Destroy для пуль
// В PlayerController.cs
private void Shoot()
{
    GameObject bullet = ObjectPoolManager.Instance.SpawnBullet(spawnPos, rotation);
    // Настройка пули...
    ObjectPoolManager.Instance.ReturnAfterDelay(bullet, 3f);
}

// В Enemy.cs
private void ShootAtPlayer()
{
    GameObject bullet = ObjectPoolManager.Instance.SpawnEnemyBullet(spawnPos, rotation);
    ObjectPoolManager.Instance.ReturnAfterDelay(bullet, bulletLifetime);
}
```

---

## 🎯 Следующие шаги:

### Рекомендации по порядку внедрения:

1. **Сначала:** ObjectPoolManager
   - Оптимизирует производительность
   - Заменяет Instantiate/Destroy

2. **Затем:** ParticleManager
   - Добавляет визуальную полировку
   - Использует pooling

3. **После:** PowerUpManager
   - Расширяет геймплей
   - Добавляет прогрессию внутри сессии

4. **Наконец:** DashAbility
   - Улучшает управление
   - Добавляет skill ceiling

---

## 🐛 Troubleshooting:

**Q: Power-ups не собираются**
A: Убедитесь что у pickup есть CircleCollider2D с IsTrigger=true и у Player есть коллайдер

**Q: Dash не работает**
A: Проверьте что DashAbility добавлен к Player и клавиша настроена

**Q: Эффекты не появляются**
A: Убедитесь что ParticleManager.Instance != null и prefabs назначены

**Q: Пулы выдают warning "exhausted"**
A: Увеличьте maxSize пула или включите expandIfNeeded

---

## 📊 Производительность:

**До оптимизации:**
- ~500 Instantiate calls/sec
- GC spikes при спавне

**После Object Pooling:**
- ~0 Instantiate calls (после инициализации)
- Стабильный FPS
- Минимальный GC

---

## 🎨 Создание префабов:

### Power-Up Prefab:
1. Sprite (CircleCollider2D IsTrigger)
2. Цветной glow
3. Легкая анимация вращения

### Ghost Trail Prefab:
1. Копия спрайта игрока
2. Полупрозрачный
3. Fade out через 0.5 сек

### Particle Prefabs:
1. Explosion: расширяющиеся частицы
2. Impact: небольшая вспышка
3. MuzzleFlash: яркая вспышка

---

Все системы готовы и полностью документированы! 🚀
